<h2>All Issues</h2>

<!-- SEARCH -->
<%= render partial: "shared/single_search" %>
<!------>


<ul>
  <% @issues.each do |i| %>
    <li>
      <%= link_to i.summary, issue_path(i.id) %>
    </li>
  <% end %>
</ul>
